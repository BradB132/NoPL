#"Let's test some animals";

Pointer anims = Animals;
Pointer allKeys = anims.allKeys;

for(int i = 0; i < allKeys.count; i++)
{
	String name = allKeys[i];
	Pointer animal = anims[name];
	#format("Animal #% is a % which is % and says '%'", i, name, (String)animal.size, (String)animal.sound);
}